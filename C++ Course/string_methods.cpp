#include <bits/stdc++.h>
using namespace std;

int main() {

    string name;
    cout << "Enter your name: ";
    getline(cin, name);
    // if (name.empty()){
    //     cout << "You didn't enter a name." << '\n';
    // }
    // else if (name.length()  > 12) {
    //     cout << "Your name can't be over 12 characters long." << '\n';
    // }
    // else {
    //     cout << "Welcome, " << name << '\n';;
    // }

    // string email = name.append("@gmail.com");
    // cout << "Your email could be: " << email << '\n'; 

    // character at indexes
    //cout << name.at(index) << '\n';

    //insert characters
    // name.insert(index, str);

    // find the position of the first occurence of a character
    // name.find(character)

    // erase a portion of a string (end position is exclusive)
    // name.erase(BeginningIndex, EndingIndex)

    return 0;
}