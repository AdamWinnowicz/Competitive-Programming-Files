#include "bits/stdc++.h"
using std::cin;
using std::cout;

int main() {
    // Arithmetic operators = return the result of a specific arithmetic operation (+ - * / %)

    // int students = 20;
    // addition
    // students = students + 1;
    // students += 1;
    // students++; 

    // subtraction
    // students = students - 1;
    // students -= 1; 
    // students--; 

    // multiplication
    // students = students * 2;
    // students *= 2;

    // division
    // students = students / 2;
    // students /= 2;

    // modulus
    // students = students % 3; // remainder of division
    // students %= 3;

    // PEMDAS = Parentheses, Exponents, Multiplication and Division (left to right), Addition and Subtraction (left to right)
    int students = 6 - 5 + 4 * 3 / 2; // 6 - 5 + 4 * 3 / 2 = 6 - 5 + 12 / 2 = 6 - 5 + 6 = 7
    cout << students << '\n';
}